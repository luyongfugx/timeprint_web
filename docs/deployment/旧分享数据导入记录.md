# 旧分享数据导入记录

已将用户提供的 watermarks_share_links_rows.csv 导入 43.156.199.205 的 timeprint_share_wm。

- 原文件：/Users/waynelu/Downloads/watermarks_share_links_rows.csv
- SHA-256：a2fe3f3ee3ed8c8ae2e849cd7aa13f2211ca94f2f35cd0825c4c2ab6323e712d
- CSV 总计 883 条，无重复 ID、无大小写归一化后的重复分享码。
- 原 ID 是 UUID，因此在确认分享及关联业务表为空后执行 003：主键与 5 个外键字段统一 CHAR(36)，新记录默认 UUID()，名称容量扩至 255。
- 6 条历史名称超过 100 字符，已完整保留。新版发布参数仍限制 100 字符。
- 在一笔事务中导入 883 条，并在提交前逐条比较全部 10 个原始字段。时间按原时区转换为 UTC，精确保留毫秒；ID、分享码、状态、期限及 URL 不变。
- 全部记录标记 contract_version=1、visibility=NULL、discovery_state=held，不自动进入公开搜索。管理员登录数据未改动。
- 原 CSV 与变更前的六张关联表及两个视图结构已备份于 /Users/waynelu/.local/share/timeprint-backups/legacy-import-1789370641083，目录 700、文件 600。
- 8 项分享数据库集成测试在本地 UUID 测试库通过，包括新分享发布、资源绑定、幂等及旧接口兼容。没有在生产库运行清理测试。

导入工具为 scripts/import-legacy-shares.mjs，需要 Python 3 解析 CSV，默认只预检查；--apply-schema 仅允许原始 bigint 空分享库；--apply 只允许空业务表，重复运行会拒绝覆盖已有记录。003 为结构变更，不能作为可回滚 DDL 处理。

Prisma schema 已与 UUID 主键同步。后续部署生成新的 Prisma Client。完整空库 SQL 已包含 003；使用分步脚本的新库需要在 001/002 后补充 003。当前线上库已执行，不要再跑 001/003 或重新导入此 CSV。

本次仅迁移数据库记录，没有下载或移动原封面、Logo 和 JSON 文件。旧码在线预览/导入还需要配置旧资源域名白名单、可用的私有 Storage 和后端域名部署；不能仅凭记录存在认定文件链路已联调完成。
