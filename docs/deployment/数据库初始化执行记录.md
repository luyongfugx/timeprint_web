# 数据库初始化执行记录

已按本次授权实际完成远程初始化。

| 项目 | 结果 |
| --- | --- |
| 实例 | 43.156.199.205:3306 |
| 数据库 | timeprint_share_wm |
| MySQL 版本 | 8.0.40 |
| 执行账号 | timeprint_app@% |
| 执行前 | 空库 |
| 分享迁移 | scripts/migrate-templates.mjs 预检查和 --apply 成功 |
| 管理员初始化 | scripts/migrate-admin-login.mjs 成功 |
| 结构验证 | 14 张基础表、2 个视图，视图可查询 |
| 默认管理员 | luyongfugx@gmail.com，admin，enabled=true |
| 业务数据 | 分享记录 0 条，资源记录 0 条；未导入 Supabase 数据 |

连接使用 sslaccept=accept_invalid_certs，按用户确认取消 CA 证书校验；这不等于强制关闭 TLS。密码仅保存在 Git 忽略、权限 600 的 .env.migration.local 和 .env.local，未写入本文。

本地 .env.local 已同步运行时连接，连接池上限 10；迁移连接池上限 1。服务器部署时需要单独设置相同目标的 DATABASE_URL，并重启应用。此次没有部署/重启远程 Web 服务，没有打开分享功能开关，也没有配置 Storage。

首次建表已经完成，不要再次运行 001 迁移或空库 SQL。后续代码部署只需生成 Prisma Client、构建与重启；结构变更另行执行增量迁移。
